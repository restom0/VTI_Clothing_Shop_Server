meta {
  name: List brands
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/brands
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("returns brands successfully", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("object");
    expect(res.body).to.have.property("object");
    expect(res.body.object).to.be.an("array");
  });
}
