meta {
  name: List products
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/product
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("returns products successfully", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("object");
    expect(res.body).to.have.property("object");
    expect(res.body.object).to.be.an("array");
  });
}
