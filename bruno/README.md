# Bruno API Tests

Start the Spring Boot app on `http://localhost:8080`, then run:

```powershell
bru run bruno --env Local
```

The collection includes public catalog checks, auth validation checks, protected-order 4xx checks, OpenAPI, and actuator health smoke tests.
